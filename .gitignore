# ─────────────────────────────────────────────────────────────
# Neovim – safe .gitignore for dotfiles repo
# ─────────────────────────────────────────────────────────────

# macOS garbage
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Neovim state / caches (never commit these)
.session
.shada
.shada/
*/.shada
*.swp
*.swo
*.swn
*~
undo/
swap/
.cache/
.local/
.data/
.spell/
.viminfo

# LSP / mason / treesitter junk (huge & machine-specific)
mason/
mason-lspconfig.nvim/
node_modules/
parser/
parsers/
lsp/
null-ls/
efm-langserver/

# Plugin manager caches & locks
.lazy-lock.json
.mini.lock
.packer.lock
lazy-lock.json
packer_compiled.lua

# Miscellaneous plugin data dirs that grow forever
**/doc/tags
**/plugged/
**/pack/
**/opt/

# Optional: if you use lazy.nvim snapshot files
.lazy-snapshots/

# Optional: if you use undo history persistence
undodir/

# Keep your actual config files!
!init.lua
!lua/
!colors/
!after/
!ftplugin/
!snippets/
!queries/
!*.vim
!*.lua
